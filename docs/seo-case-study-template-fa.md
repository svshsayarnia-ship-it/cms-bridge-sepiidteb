# مطالعه موردی سئو سپید بیوتی

> این سند فقط با داده‌های واقعی Search Console و Analytics تکمیل می‌شود. عدد تخمینی یا نتیجه‌ی بدون مدرک وارد گزارش نشود.

## خلاصه پروژه

- نام پروژه: سپید بیوتی
- دامنه: https://www.sepiidbeauty.ir
- نوع کسب‌وکار: فروشگاه و مرجع شناخت محصولات حرفه‌ای زیبایی
- بازه بررسی: `تاریخ شروع` تا `تاریخ پایان`
- نقش اجراکننده: سئو، معماری محتوا، مدیریت محصول و بهینه‌سازی تجربه کاربری

## وضعیت قبل از اجرا

| شاخص | مقدار واقعی قبل از اجرا | منبع | تاریخ ثبت |
| --- | --- | --- | --- |
| کلیک ارگانیک ۲۸ روزه | `ثبت شود` | Search Console | `ثبت شود` |
| ایمپرشن ارگانیک ۲۸ روزه | `ثبت شود` | Search Console | `ثبت شود` |
| میانگین رتبه | `ثبت شود` | Search Console | `ثبت شود` |
| صفحات ایندکس‌شده | `ثبت شود` | Search Console | `ثبت شود` |
| صفحات دارای خطای مهم | `ثبت شود` | Search Console / خزیدن | `ثبت شود` |

## مشکلات شناسایی‌شده

- `مشکل فنی یا ایندکس`
- `مشکل معماری دسته‌بندی`
- `مشکل محتوای محصول یا بسته‌بندی`
- `مشکل لینک‌سازی داخلی`
- `مشکل تجربه کاربری یا مسیر استعلام`

## اقدامات P0 انجام‌شده

### صفحات دسته‌بندی

- فیلرها و ژل‌های حجم‌دهنده: `آدرس و تاریخ`
- مزوژل و اسکین‌بوستر: `آدرس و تاریخ`
- کوکتل‌های تخصصی: `آدرس و تاریخ`

### داده‌های محصولات

- مدل، حجم و تعداد بسته از هم تفکیک شد: `جزئیات`
- قیمت و وضعیت موجودی با منبع اصلی تطبیق شد: `جزئیات`
- تصویر مدل‌های مختلف با وضعیت واقعی تصویر مشخص شد: `جزئیات`
- صفحات محصول و کارت‌های دسته‌بندی از یک منبع داده استفاده می‌کنند: `جزئیات`

### پایش و صحت‌سنجی

- سایت‌مپ و robots بررسی شد: `تاریخ`
- لینک‌های داخلی و کنونیکال بررسی شد: `تاریخ`
- گزارش قبل و بعد در Search Console ثبت شد: `تاریخ`

## نتایج بعد از اجرا

| شاخص | قبل | بعد | تغییر | منبع |
| --- | ---: | ---: | ---: | --- |
| کلیک ارگانیک | `ثبت شود` | `ثبت شود` | `محاسبه شود` | Search Console |
| ایمپرشن | `ثبت شود` | `ثبت شود` | `محاسبه شود` | Search Console |
| میانگین رتبه | `ثبت شود` | `ثبت شود` | `محاسبه شود` | Search Console |
| صفحات ایندکس‌شده | `ثبت شود` | `ثبت شود` | `محاسبه شود` | Search Console |
| استعلام یا کلیک تماس | `ثبت شود` | `ثبت شود` | `محاسبه شود` | Analytics / CRM |

## نکته‌های صداقت گزارش

- هر رتبه با عبارت جست‌وجو، کشور، دستگاه و تاریخ ثبت شود.
- «رتبه یک» بدون ذکر عبارت دقیق و تاریخ، قابل استناد نیست.
- رشد ایمپرشن به‌تنهایی معادل رشد فروش نیست.
- اگر نتیجه‌ای هنوز قطعی نیست، با عبارت «در حال پایش» ثبت شود.
